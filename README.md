# gwel
Budget-aware active perception for sub-1B VLMs: a lightweight router picks the cheapest visual action per query (answer low-res, crop, or OCR) under real memory, latency and energy constraints. Oracle study + supervised router + reproducible benchmark.
